{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "documind.serviceAccountName" . }}
  labels:
    {{- include "documind.labels" . | nindent 4 }}
{{- end }}
